function getRandomNum(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min; // Получение случайного целого числа в заданном интервале, включительно. Максимум и минимум включаются
  }